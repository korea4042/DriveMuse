org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# YouTube Data API v3 key. Leave blank here and put the real value in
# ~/.gradle/gradle.properties so it never reaches version control.
ytApiKey=

# Optional external services; real values belong in user Gradle properties.
geminiModel=
weatherApiKey=
